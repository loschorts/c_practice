#include <stdio.h>

int main(){
	printf("%d", (1 == 0) + 2);
	// int c;
	// int lim = 5;
	// for ( int i=0 ; ((i < lim-1) + ((c=getchar()) != '\n') + (c != EOF)) == 3; ++i) {
	// 	printf("%c", c);
	// }

}